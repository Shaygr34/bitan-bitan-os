{
  "name": "bitan-bitan-os",
  "private": true,
  "packageManager": "pnpm@10.18.1",
  "scripts": {
    "dev:os-hub": "pnpm --filter os-hub dev",
    "build:os-hub": "pnpm --filter os-hub build",
    "start:os-hub": "pnpm --filter os-hub start",
    "lint": "pnpm --filter os-hub lint"
  }
}
